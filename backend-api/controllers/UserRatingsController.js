const {db} = require ('../db');
const Utilities = require('./Utilities');
const UUID = require('uuid');

exports.create = async (req, res) => {
    if (req.body.UserScore || !req.body.UserScore) {

    }
}